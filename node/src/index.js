import React from 'react';
import ReactDOM from 'react-dom';

function App() {
    return (
        <div>
            <h1>Bem-vindo ao Paraninfo!</h1>
            <p>Esta página foi criada usando Node.js e React.</p>
        </div>
    );
}

ReactDOM.render(<App />, document.getElementById('root'));